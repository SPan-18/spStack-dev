{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Install Package",
            "type": "shell",
            "command": "R CMD build . && R CMD INSTALL --preclean --no-multiarch --with-keep.source spStack_0.1.0.tar.gz",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}
